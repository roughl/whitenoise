README
This Readme is about the whitenoise-pictures-generation-tool by Lucien Buchmann & Co.

OUTPUTFILES
folder "output" with the desired amount <f> subdirectories which all contain <p> pictures

MAYBE INTERESTING
- start the program from console if possible, so you can read the error-messages

USAGE
./whitenoise
./whitenoise -x 200 -y 100 -f 2 -p 10
x: width (default: 960)
y: height (default: 960)
f: amount of folders (default: 1)

and then:
./convert.sh

NEEDED PACKAGES
ubuntu example: sudo apt-get install libsdl1.2-dev libsdl-image1.2-dev libtclap-dev

COMPILE
make

